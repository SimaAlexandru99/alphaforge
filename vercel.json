{
  "buildCommand": "pnpm exec prisma db push --schema ./prisma/schema.prisma && pnpm exec tsx scripts/seed-demo.ts && pnpm run build",
  "installCommand": "pnpm install",
  "crons": [
    {
      "path": "/api/cron/agent-tick",
      "schedule": "0 12 * * *"
    }
  ]
}